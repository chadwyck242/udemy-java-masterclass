public class PositiveNegativeZero {
    public static void checkNumber (int number) {
        // Based on the course example provided by the instructor
        System.out.println((number > 0) ? "positive": ((number < 0) ? "negative" : "zero"));
    }
}
